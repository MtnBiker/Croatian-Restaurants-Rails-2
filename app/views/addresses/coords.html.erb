<h1>Finding coordinates for an address</h1>
<h3>Using Mapbox</h3>
 
<%= form_for :address, url: address_path(@address), method: :patch do |f| %>
 
  <% if @address.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@address.errors.count, "error") %> prohibited
        this address from being saved:
      </h2>
      <ul>
        <% @address.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  # <p>
  #   <%= f.label :title %><br>
  #   <%= f.text_field :title %>
  # </p>
  #
  # <p>
  #   <%= f.label :text %><br>
  #   <%= f.text_area :text %>
  # </p>
 
  <%= render 'form' %>

  <%= link_to 'Show', @address %> |
  <%= link_to 'Back', addresses_path %>
  <p>
    <%= f.submit %>
  </p>
 
<% end %>
 
<%= link_to 'Back', addresses_path %>